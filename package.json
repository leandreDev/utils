{
  "name": "@hfdev/utils",
  "version": "1.0.151",
  "description": "utilitaire",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "tsc",
    "testing": "node dist/test.js",
    "rmDist": "rm -r dist",
    "devDistToMaster": "git checkout dev -- dist",
    "updateMaster": " npm run rmDist && npm run devDistToMaster",
    "version": "npm run updateMaster && git add -A ",
    "postversion": "export minorTag=$(node -e \"console.log(process.env.npm_package_version.split('.')[1]);\") && export majorTag=$(node -e \"console.log(process.env.npm_package_version.split('.')[0]);\") && git tag -f $majorTag &&  git tag -f $majorTag.$minorTag && git push && git push -f --tags "
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/HF-dev/utils.git"
  },
  "author": "hf-dev",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/HF-dev/utils/issues"
  },
  "homepage": "https://github.com/HF-dev/utils#readme",
  "peerDependencies": {
    "assert": "^1.4.1",
    "express": "^4.16.1",
    "fs-extra": "^3.0.1",
    "lodash": "^4.17.20",
    "moment": "^2.22.2",
    "mongodb": "^3.5.6",
    "node-jose": "^2.0.0",
    "request": "^2.81.0",
    "request-promise-native": "^1.0.5"
  }
}
